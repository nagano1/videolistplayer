package org.rokist.videolistplayer.util

/*
import androidx.work.Data
import androidx.work.Worker
import androidx.work.WorkerParameters
import org.eclipse.jgit.api.Git
import org.eclipse.jgit.lib.ProgressMonitor
import org.kohsuke.github.GitHub
import org.kohsuke.github.GitHubBuilder
import java.io.File


class MyWorker(ctx: Context, params: WorkerParameters) : Worker(ctx, params) {

    private val KEY_RESULT = "jofaiwe"

    override fun doWork(): Result {
        Log.d("aaa", "dop");

        val output: Data = androidx.work.Data.Builder()
            .putInt(KEY_RESULT, 1111235)
            .build()
        return Result.success(output)

        //return Result.failure()// Result.success()
    }

}
*/